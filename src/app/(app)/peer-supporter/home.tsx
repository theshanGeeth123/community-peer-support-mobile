import RoleTabPlaceholder from "@/features/navigation/components/RoleTabPlaceholder";

export default function PeerSupporterHomeScreen() {
  return (
    <RoleTabPlaceholder
      title="Peer Supporter Home"
      description="View assigned groups, pending requests and important support activities."
      icon="home-outline"
    />
  );
}