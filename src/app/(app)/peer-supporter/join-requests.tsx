import RoleTabPlaceholder from "@/features/navigation/components/RoleTabPlaceholder";

export default function PeerSupporterJoinRequestsScreen() {
  return (
    <RoleTabPlaceholder
      title="Join Requests"
      description="Review, approve or reject users who request to join assigned groups."
      icon="person-add-outline"
    />
  );
}